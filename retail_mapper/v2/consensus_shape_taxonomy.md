# Consensus Shape Taxonomy Overrides

Approved rows collapse mutually exclusive shape stacks into one canonical shape identity.

Approved overrides: `467`
Review rows: `0`

## Issue Counts

- `pasta_shape_sibling_stack`: `467`